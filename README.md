# 🌤️ Chat Weather Bot

Telegram-бот для аналитики группового чата с использованием LLM (Gemini) для анализа пользователей и веб-интерфейсом на Next.js.

## 📋 Требования

- Docker и Docker Compose
- Telegram Bot Token (получить у @BotFather)
- Gemini API Key (получить на https://aistudio.google.com/)

**⚠️ Важно:** Бесплатный тариф Gemini API имеет лимит 20 запросов/день. При превышении лимита функции анализа и погоды в чате будут недоступны до сброса лимита (обычно через 24 часа) или обновления тарифного плана.

## 🚀 Быстрый запуск

### 1. Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_telegram_bot_token_here
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-3-flash-preview
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=telegram_analytics
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/telegram_analytics
REDIS_URL=redis://redis:6379
CACHE_TTL=1200
```

### 2. Запустите проект:

```bash
./rebuild.sh
```

Или вручную:
```bash
sudo docker compose up --build -d
```

### 3. Добавьте бота в группу и отправьте `/start`

Веб-интерфейс: http://localhost:3000

## 📱 Команды бота

- `/stats` - статистика чата (топ пользователей, фильтры по времени)
- `/analyze @username` - анализ пользователя через Gemini AI
- `/weather` или `/погода` - погода в чате (тональность сообщений)

## 🌤️ Дополнительная фича: Погода в чате

**Описание:** Анализирует тональность сообщений (позитивные/негативные/нейтральные) за разные периоды и определяет самых позитивных и негативных пользователей.

В отсутствии токенов хорошо распознаёт тональность только английского языка через библиотеку sentiment.

**Использование:**
1. Отправьте `/weather` или `/погода` боту
2. Выберите период (сегодня, неделя, месяц, всё время)
3. Получите анализ настроения в чате с эмодзи погоды:
   - ☀️ - Солнечно и позитивно (>60% позитивных)
   - ⛅ - В основном ясно (40-60% позитивных)
   - 🌤️ - Переменная облачность (20-40% позитивных)
   - ☁️ - Нейтрально
   - 🌧️ - Дождливо и негативно (>40% негативных)
   - ⛈️ - Гроза (20-40% негативных)

**Особенности:**
- Анализ до 100 сообщений за период
- Определение главного позитивщика и негативщика
- Кэширование результатов на 20 минут
- Автоматическая очистка кэша при новых сообщениях

## 🔧 Скрипты

- `./rebuild.sh` - пересборка и запуск проекта
- `./clean-build.sh` - полная очистка и пересборка (при проблемах)

## 🛑 Остановка

```bash
sudo docker compose down
```
